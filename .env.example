# Copy to .env (never commit .env) or export these in your shell profile.
TYPESAFE_API_KEY=
# Optional: a /v1/systemone-compatible gateway instead of TypeSafe direct
# TYPESAFE_BASE_URL=https://api.typesafe.ai
# Optional: pin a model version so thresholds stay stable
# TYPESAFE_MODEL=jev-1.13.0
