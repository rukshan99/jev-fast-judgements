## Summary

## Checklist
- [ ] `python tools/validate.py` passes
- [ ] `python -m unittest discover -s tests -v` passes
- [ ] SKILL.md stays under 500 lines; long material goes in `references/`
- [ ] No new runtime dependencies (the script is standard library only)
- [ ] Version bumped in `plugin.json`, `marketplace.json` and `CHANGELOG.md` (for releases)
